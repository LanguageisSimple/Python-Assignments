#Taking input from user and storing it in a variable named as 'a'
a = int(input("Enter the first number : "))

#Taking another input from user and storing it in a variable named as 'b'
b = int(input("Enter the second number :"))

#Using the '/=' operator between the two variables, 'a' and 'b'
a/=b

#Printing the result a
print(a)
