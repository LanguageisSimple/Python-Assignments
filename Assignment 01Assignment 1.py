#Taking input from user in floating point datatype
a = float(input("Enter a number : "))

#Storing the integer value of a in another variable name 'b'
b = int(a)

#Printing the value of b which is the integer value of a
print(b)
